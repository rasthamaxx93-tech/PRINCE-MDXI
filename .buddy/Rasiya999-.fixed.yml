- pipeline: rasiya999
  name: Rasiya999
  refs:
  - refs/heads/main
  fail_on_prepare_env_warning: true
  events:
  - type: WEBHOOK
  actions:
  - action: npm start
    type: BUILD
    commands: |-
      # yarn install
      npm install
      npm start
    shell: BASH
    docker_image_name: library/node
    docker_image_tag: 24
